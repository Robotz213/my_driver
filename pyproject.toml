[project]
name = "my-driver"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "packaging>=26.0",
    "selenium>=4.41.0",
    "selenium-wire>=5.1.0",
]

[tool.setuptools]
package-dir = { "my_driver" = "my_driver" }
